import 'package:flutter/material.dart';

// test of git: VCS -> Commit... -> Message -> Commit (arrow) -> Commit and push

void main() {
  runApp(
    MaterialApp(
      home: Scaffold(
        backgroundColor: Colors.teal,
        body: Container(),
      ),
    ),
  );
}
